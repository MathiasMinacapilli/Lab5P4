//Archivos
#include "../include/Etapa.hpp"
/* Ver que si está en un estado final que no se muestre la opcion al usuario
para realizar una operacion. Por ejemplo si el pedido ya está en 
cancelado que no muestre nada. Solo cambia de estado si el estado es valido. */
Etapa::Etapa(){
}
